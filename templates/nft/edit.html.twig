{% extends 'base.html.twig' %}

{% block title %}Éditer {{ nft.name }}{% endblock %}

{% block pagecontent %}
    <h1>Éditer {{ nft.name }}</h1>

    {{ include('nft/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('nft/_delete_form.html.twig') }}

    <a class='btnToBack' href="{{ path('app_nft_index') }}">retour</a>
{% endblock %}
