{% extends 'base.html.twig' %}

{% block title %}Anthology{% endblock %}

{% block body %}
    <h1>Anthology</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ anthology.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ anthology.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_anthology_index') }}">back to list</a>

    <a href="{{ path('app_anthology_edit', {'id': anthology.id}) }}">edit</a>

    {{ include('anthology/_delete_form.html.twig') }}
{% endblock %}
