{% extends 'base.html.twig' %}

{% block title %}SubCategory
{% endblock %}

{% block pagecontent %}
	<h1>SubCategory</h1>
	<div class="tableGeneral ShowTable">
		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ sub_category.id }}</td>
				</tr>
				<tr>
					<th>Name</th>
					<td>{{ sub_category.name }}</td>
				</tr>
				<tr>
					<th>Description</th>
					<td>{{ sub_category.description }}</td>
				</tr>
			</tbody>
		</table>
	</div>
	<div class="bottomBtnShow">
		<a class="btnToBack" href="{{ path('app_sub_category_index') }}">Retour</a>

		<a class="btnToBack" href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}">Éditer</a>

		{{ include('sub_category/_delete_form.html.twig') }}
	</div>
{% endblock %}
