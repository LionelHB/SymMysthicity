{% extends 'base.html.twig' %}

{% block title %}Éditer {{ user.username }}{% endblock %}

{% block pagecontent %}
    <h1>Éditer {{ user.username }}</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('user/_delete_form.html.twig') }}

    <a class='btnToBack' href="{{ path('app_user_index') }}">retour</a>
{% endblock %}
