{% extends 'base.html.twig' %}

{% block title %}Éditer "{{ category.name }}{% endblock %}

{% block pagecontent %}
    <h1>Éditer "{{ category.name }}"</h1>

    {{ include('category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('category/_delete_form.html.twig') }}

    <a class='btnToBack' href="{{ path('app_category_index') }}">retour</a>
{% endblock %}
