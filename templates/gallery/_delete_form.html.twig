<form method="post" action="{{ path('app_gallery_delete', {'id': gallery.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gallery.id) }}">
    <button class="deleteBtn">Supprimer</button>
</form>
