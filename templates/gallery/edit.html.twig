{% extends 'base.html.twig' %}

{% block title %}Éditer "{{ gallery.name }}"{% endblock %}

{% block pagecontent %}
    <h1>Éditer "{{ gallery.name }}"</h1>

    {{ include('gallery/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('gallery/_delete_form.html.twig') }}

    <a class='btnToBack' href="{{ path('app_gallery_index') }}">retour</a>
{% endblock %}
